<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    SQL File Name : FileGrpDaoImpl.xml
    Description : 파일그룹 관련
-->
<mapper namespace="com.kbrainc.plum.cmm.file.model.FileGrpDao">

	<!-- 파일그룹 등록 -->
	<insert id="newFileGrp" useGeneratedKeys="true" keyProperty="filegrpid" parameterType="FileGrpVo">
	    /* FileGrpDao.newFileGrp */
		INSERT INTO TB_CMM_FILEGRP
		(
			FILEGRP_NM,
            BBSID,
			MDFCN_DT,
			MDFRID,
			REG_DT,
			REGUSERID
		)
		VALUES (
			#{filegrpNm},
            #{bbsid},
			NOW(),
			#{mdfrid},
			NOW(),
			#{reguserid}
		)
	</insert>
	
	<!-- 파일그룹 삭제 -->
	<delete id="deleteFileGrp" parameterType="FileGrpVo">
	    /* FileGrpDao.deleteFileGrp */
		DELETE
		FROM TB_CMM_FILEGRP
		WHERE FILEGRPID = #{filegrpid}
	</delete>
</mapper>